import { configRoutes } from "@/utils/route";
import styled from "styled-components";

export const DelegatorContainer = styled.div``;

export const TitleBase = styled.div`
  color: #fff;
  width: 80%;
  margin: 0 auto 20px auto;
  font-size: 48px;
  padding-top: 60px;
  font-weight: bold;
`;
export const DataListBase = styled.pre`
  background: #121212;
  border-radius: 12px;
  font-size: 14px;
  line-height: 18px;
  padding: 30px;
  color: aqua;
  white-space: pre-wrap;
  overflow: auto;
  font-family: inherit;
  margin: 20px 0px;
`;
