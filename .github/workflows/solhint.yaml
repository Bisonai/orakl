name: Solhint
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn
        working-directory: ./contracts
      - run: yarn solhint
        working-directory: ./contracts
