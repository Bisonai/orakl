version: "3"

tasks:
  test-health:
    cmds:
      - go test ./pkg/checker/health -v
  test-balance:
    dotenv: [".env"]
    cmds:
      - go test ./pkg/checker/balance -v
  test:
    cmds:
      - task: test-health
      - task: test-balance
  run:
    dotenv: [".env"]
    cmds:
      - go run ./cmd/main.go
