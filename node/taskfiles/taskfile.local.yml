version: "3"

tasks:
  test-db:
    dotenv: [".env"]
    cmds:
      - go test ./pkg/db -v
  test-admin:
    dotenv: [".env"]
    cmds:
      - go test ./pkg/admin/tests -v
  test-bus:
    cmds:
      - go test ./pkg/bus -v
  test-libp2p:
    cmds:
      - go test ./pkg/libp2p -v
